# Supabase backend integration

This directory exists so the backend repo can use the Supabase CLI for local development, schema management, and future project linking.

## Source of truth

The SQL migrations are maintained in:

- `../migrations`

These files are mirrored into `supabase/migrations` so the Supabase CLI can operate from this repo.

## Current project ref

- `oknufrwnszsbptusnusa`

## Still needed to complete `supabase link`

- Supabase database password for the project, or
- a Supabase access token for CLI authentication

